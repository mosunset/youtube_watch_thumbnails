{
  "name": "youtube_watch_thumbnails",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "dev": "turbo run dev --parallel --continue",
    "dev:extension": "turbo run dev --filter=extension",
    "dev:docs": "turbo run dev --filter=docs",
    "build": "turbo run build --continue",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
